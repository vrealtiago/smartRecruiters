## Installation of dependencies of the Project
Run `npm install` for install all dependencies

## Running Project
Run `npm start` for run the project.

## Running test
Before run the `Test` the app needs to be started `npm start` and then run `npm test`.
